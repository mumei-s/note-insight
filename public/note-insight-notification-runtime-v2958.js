(function(){
'use strict';
if(location.hostname!=='note.com')return;
if(window.__mumeiNotificationRuntime2958)return;window.__mumeiNotificationRuntime2958=true;
// Compatibility shim only. Visible notification UI is owned exclusively by
// note-insight-notification-dock-watch-v312.js (clean panel controller).
// Keep this filename alive because older installed loaders still request it.
})();
