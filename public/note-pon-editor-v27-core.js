(() => {
'use strict';
const page=typeof unsafeWindow!=='undefined'?unsafeWindow:window;
if(page.__MUMEI_PON_V27_CORE__)return;page.__MUMEI_PON_V27_CORE__=true;
['__MUMEI_PON_V15_ADDON__','__MUMEI_PON_V16_ADDON__','__MUMEI_PON_V171_ADDON__','__MUMEI_PON_V172_ADDON__','__MUMEI_PON_V173_ADDON__','__MUMEI_PON_V18__','__MUMEI_PON_V181__','__MUMEI_PON_V19__','__MUMEI_PON_V20__','__MUMEI_PON_V21__','__MUMEI_PON_V22__','__MUMEI_PON_V23_CORE__','__MUMEI_PON_V24_CORE__','__MUMEI_PON_V25_CORE__','__MUMEI_PON_V26_CORE__'].forEach(k=>page[k]=true);

const SAVED='https://raw.githubusercontent.com/mumei-s/note-insight/main/public/note-pon-editor-v15.user.js';
const sleep=ms=>new Promise(r=>setTimeout(r,ms));
const EXKEY=new Set(['m535c97031825','m7ffeddfdfb3c']);
const EXTITLE=/(コメントできなくなりました|ちび創作大賞|スキ動画コンテスト)/;
const COUNT={mf534419e7479:10,m300d06308833:10,m9872a92a8af5:10,mbefcb2e5a397:5,m1df0b906bace:5,m74b154cd7893:5,m653e8e82ea44:5,m4d9b9698cacf:5,mb028566a46ee:5,md96759f4be5b:5,mb80f5e0f9b99:5,m3759ff7a5b9c:5,m9e01fdb0606f:5,ma475a8bdcecc:5,m254cc8180f92:5,mba58a6f9aacf:5,m9bb45783969e:5,m33495b5ea807:5,m752f734f7a1c:4,mef2032492c4a:3,mb3dc2cd9766e:3,macfef0fcc489:3,m5db97d398203:3,mc4827a8e939b:3,m9186cf842d83:3,me86c388d3826:3,m95b78222e9b9:3,m16580951510b:3,ma4dad1f25900:3,mf2e99e9aa411:2,mc9bf7875a8d7:2,meadce3d098b0:2,mf7c9271b4e5e:2,m8d6e2d4322c8:2,m4eb9deb52a78:1,mf21f18654494:1,md3a807653baf:1,mbe79c0d9105c:1,m9f1b6d83fe39:1,ma7a2c6649fa2:1};
const UNLIMITED=new Set(['mff26de4b50e8','m3a58ed12c332','ma8d107d9475f','m6cf909200081','mad3a5537da46','m97848c1bdf32']);
const PAID=new Set(['mb4495066c358']);
// 固定記事はURLまで確認できたものだけ。自動推測は禁止。
const FIXED={
 mff26de4b50e8:'https://note.com/mameshibamendako/n/n5001ef7fe6b7',
 ma8d107d9475f:'https://note.com/tatsuopapa/n/n6a405735ec8d',
 m300d06308833:'https://note.com/ayumu_ai/n/nc12952774db5',
 m1df0b906bace:'https://note.com/sui_ai_drawing/n/n7f5bc24a0753',
 m74b154cd7893:'https://note.com/ss_yr/n/n8fc89fe2155e',
 m32528b24bba4:'https://note.com/ss_yr/n/n00139a00c0f4'
};
let busy=false,viewCache=null,selectionCache=null,noteUrlCommand=null;

const normalize=value=>{try{const u=new URL(String(value||''),location.href);u.search='';u.hash='';return u.href}catch{return String(value||'').trim()}};
const mkey=u=>(String(u).match(/\/m\/(m[a-z0-9]+)/i)||[])[1]||'';
const owner=u=>{try{return new URL(u).pathname.split('/').filter(Boolean)[0]||''}catch{return''}};
const isMag=u=>/^https:\/\/note\.com\/[^/]+\/m\/m[a-z0-9]+$/i.test(String(u||''));
const isArt=u=>/^https:\/\/note\.com\/[^/]+\/n\/n[a-z0-9]+$/i.test(String(u||''));
const isNote=u=>isMag(u)||isArt(u);
const xhr=url=>new Promise((resolve,reject)=>GM_xmlhttpRequest({method:'GET',url,timeout:30000,onload:r=>r.status>=200&&r.status<300?resolve(r.responseText):reject(new Error(`GET ${r.status}`)),onerror:()=>reject(new Error('通信失敗')),ontimeout:()=>reject(new Error('通信タイムアウト'))}));
function savedRows(src){const m=String(src).match(/const FALLBACK=`([\s\S]*?)`\.trim\(\)\.split/);if(!m)return[];const seen=new Set();return m[1].trim().split('\n').map((line,index)=>{const p=line.lastIndexOf('|');if(p<1)return null;const title=line.slice(0,p).trim(),url=normalize(line.slice(p+1).trim()),key=mkey(url);if(!key||!isMag(url)||seen.has(key)||EXKEY.has(key)||EXTITLE.test(title))return null;seen.add(key);return{title,url,index,key,owner:owner(url)}}).filter(Boolean)}
async function rows(){return savedRows(await xhr(SAVED))}
function ruleFor(row){if(UNLIMITED.has(row.key))return{type:'unlimited'};if(row.key in COUNT)return{type:'count',count:COUNT[row.key]};if(row.owner==='ss_yr'){const m=String(row.title).match(/^([1-9])️⃣/);if(m)return{type:'count',count:+m[1]}}return{type:'none'}}
const gkey=r=>r.rule.type==='count'?r.rule.count:r.rule.type;
const glabel=g=>g==='unlimited'?'♾️ 無制限・制限なし':g==='none'?'❓ 制限数の表記を確認できないマガジン':`${g===10?'🔟':g+'️⃣'} 1日${g}記事まで`;
function transmissionOrder(t){return /^トランスミッション$/.test(t)?1:/トランスミッション[２2]$/.test(t)?2:/トランスミッション[３3]$/.test(t)?3:0}
function ordered(items){const a=[...items].sort((x,y)=>x.index-y.index),tr=a.filter(x=>transmissionOrder(x.title)).sort((x,y)=>transmissionOrder(x.title)-transmissionOrder(y.title));if(tr.length<2)return a;const first=Math.min(...tr.map(x=>a.indexOf(x))),rest=a.filter(x=>!tr.includes(x));rest.splice(first,0,...tr);return rest}
function build(input){const items=input.map(row=>({...row,rule:ruleFor(row),pin:FIXED[row.key]||'',display:row.owner==='ss_yr'?`👑 ${row.title}`:row.title,paid:PAID.has(row.key)}));const groups=new Map();for(const x of items){const g=gkey(x);if(!groups.has(g))groups.set(g,[]);groups.get(g).push(x)}const nums=[...groups.keys()].filter(x=>typeof x==='number').sort((a,b)=>b-a),order=[];if(groups.has('unlimited'))order.push('unlimited');order.push(...nums);if(groups.has('none'))order.push('none');const out=[];for(const g of order){out.push('# '+glabel(g),'');for(const x of ordered(groups.get(g))){out.push('## '+x.display,'');if(x.paid)out.push('※有料記事追加不可','');out.push(`マガジンURL：${x.url}`,x.url,'');if(x.pin)out.push(`固定記事URL：${x.pin}`,x.pin,'');out.push('---','')}}return{items,source:out.join('\n').replace(/\n{3,}/g,'\n\n').trim()}}

function editor(){return document.querySelector('.ProseMirror[contenteditable="true"]')||document.querySelector('.ProseMirror')}
function looksLikeView(v){try{return!!(v&&typeof v==='object'&&v.state?.doc&&v.state?.schema&&typeof v.dispatch==='function'&&v.dom&&typeof v.posAtDOM==='function')}catch{return false}}
function findView(){if(looksLikeView(viewCache)&&viewCache.dom?.isConnected)return viewCache;const root=editor();if(!root)return null;const seen=new Set(),queue=[];let seed=root;for(let i=0;i<6&&seed;i++,seed=seed.parentElement)queue.push([seed,0]);let steps=0;while(queue.length&&steps++<14000){const[v,d]=queue.shift();if(!v||seen.has(v))continue;seen.add(v);if(looksLikeView(v))return(viewCache=v);let keys=[];try{keys=Object.getOwnPropertyNames(v)}catch{continue}for(const k of keys){if(['window','document','ownerDocument','parentNode','children','childNodes','style'].includes(k))continue;let x;try{x=v[k]}catch{continue}if(looksLikeView(x))return(viewCache=x);if(d<7&&x&&(typeof x==='object'||typeof x==='function')&&x!==page&&x!==document)queue.push([x,d+1])}}return null}
function webpackRequire(){const chunks=page.webpackChunk_N_E;if(!chunks||typeof chunks.push!=='function')return null;let req=null;try{chunks.push([[995000000+Math.floor(Math.random()*4000000)],{},r=>{req=r}])}catch{}return req}
function selectionApi(){if(selectionCache)return selectionCache;const req=webpackRequire();if(!req)throw new Error('note内部Selectionを取得できません');let mod;try{mod=req(44044)}catch{}const Selection=mod?.Y1;if(typeof Selection?.near!=='function')throw new Error('note Selection.nearが見つかりません');return(selectionCache=Selection)}
function noteUrlCommandFactory(){if(typeof noteUrlCommand==='function')return noteUrlCommand;const req=webpackRequire();if(!req)throw new Error('note内部URL処理を取得できません');let mod;try{mod=req(94928)}catch{}let candidate=typeof mod?.fjT==='function'?mod.fjT:null;const looksRight=value=>{if(typeof value!=='function')return false;let s='';try{s=Function.prototype.toString.call(value)}catch{}return s.includes('state.selection')&&s.includes('nodeBefore')&&s.includes('replaceRangeWith')&&s.includes('.then')};if(!looksRight(candidate)){const loaded=Object.values(req.c||{}).flatMap(e=>{const ex=e?.exports;if(typeof ex==='function')return[ex];return ex&&typeof ex==='object'?Object.values(ex):[]});candidate=loaded.find(looksRight)||null}if(!looksRight(candidate))throw new Error('note正規URLコマンドが見つかりません');return(noteUrlCommand=candidate)}
function embedNodes(view){const out=[];view.state.doc.descendants((node,pos)=>{if(node.type?.name==='embed')out.push({node,pos})});return out}
function cardKey(hit){return String(hit?.node?.attrs?.embeddedContentKey||'')}
function cardUrl(hit){return normalize(hit?.node?.attrs?.src)}
function genuineCard(hit,url){const key=cardKey(hit),html=String(hit?.node?.attrs?.htmlForEmbed||'');return cardUrl(hit)===normalize(url)&&/^emb[a-z0-9]+$/i.test(key)&&html.includes('note-embed')}
function rawRows(view,url=null){const wanted=url?normalize(url):null,out=[];view.state.doc.descendants((node,pos)=>{if(!node.isTextblock)return;const value=normalize((node.textContent||'').trim());if((wanted&&value===wanted)||(!wanted&&isNote(value)))out.push({node,pos,url:value})});return out}
function deleteHits(view,hits){if(!hits?.length)return 0;let tr=view.state.tr;[...hits].sort((a,b)=>b.pos-a.pos).forEach(h=>{tr=tr.delete(h.pos,h.pos+h.node.nodeSize)});view.dispatch(tr.scrollIntoView());view.focus();return hits.length}
function deleteDuplicateRaw(view){const groups=new Map();for(const h of rawRows(view)){if(!groups.has(h.url))groups.set(h.url,[]);groups.get(h.url).push(h)}const del=[];for(const hs of groups.values())del.push(...hs.slice(1));return deleteHits(view,del)}
function setSelectionOnRaw(view,hit){const Selection=selectionApi();const end=Math.max(1,Math.min(view.state.doc.content.size,hit.pos+hit.node.nodeSize-1));const $pos=view.state.doc.resolve(end);const sel=Selection.near($pos,-1);view.dispatch(view.state.tr.setSelection(sel).scrollIntoView());view.focus()}
async function waitForInPlaceCard(view,url,beforeKeys,aroundPos,timeout=45000){const deadline=Date.now()+timeout;while(Date.now()<deadline){const candidates=embedNodes(view).filter(h=>{const key=cardKey(h);return key&&!beforeKeys.has(key)&&genuineCard(h,url)}).sort((a,b)=>Math.abs(a.pos-aroundPos)-Math.abs(b.pos-aroundPos));if(candidates[0])return candidates[0];await sleep(250)}return null}
async function cardifyOne(view,url){const hit=rawRows(view,url).sort((a,b)=>a.pos-b.pos)[0];if(!hit)return true;const beforeKeys=new Set(embedNodes(view).map(cardKey).filter(Boolean));const originalPos=hit.pos;setSelectionOnRaw(view,hit);const command=noteUrlCommandFactory()(url);const handled=command(view.state,tr=>view.dispatch(tr),view);if(!handled)throw new Error('note正規URLコマンド未処理');const card=await waitForInPlaceCard(view,url,beforeKeys,originalPos,45000);if(!card)throw new Error('その場カード生成タイムアウト');if(Math.abs(card.pos-originalPos)>4)throw new Error(`カード位置ずれ ${card.pos-originalPos}`);return true}
async function cardifyAll(show){const view=findView();if(!view)throw new Error('EditorViewなし');selectionApi();noteUrlCommandFactory();const dup=deleteDuplicateRaw(view);if(dup)show(`🧹 重複URL ${dup}件削除`);const urls=[...new Set(rawRows(view).map(x=>x.url))];for(let i=0;i<urls.length;i++){show(`その場カード ${i+1}/${urls.length} 生成中… ${isArt(urls[i])?'固定記事':'マガジン'}`);await cardifyOne(view,urls[i]);show(`その場カード ${i+1}/${urls.length} ✅`);if(i<urls.length-1)await sleep(900)}return rawRows(view).length}

function textNode(schema,text){return text?schema.text(text):null}
function nodeForLine(schema,t){if(/^#\s+/.test(t))return schema.nodes.heading.create({level:2},textNode(schema,t.replace(/^#\s+/,'')));if(/^##\s+/.test(t))return schema.nodes.heading.create({level:3},textNode(schema,t.replace(/^##\s+/,'')));if(/^-{3,}$/.test(t)){const hr=schema.nodes.horizontal_rule||schema.nodes.horizontalRule||schema.nodes.hr;if(!hr)throw new Error('区切り線ノードなし');return hr.create()}const p=schema.nodes.paragraph;if(!p)throw new Error('paragraphなし');return p.create(null,textNode(schema,t))}
function insertSource(source){const view=findView();if(!view)throw new Error('EditorViewなし');const schema=view.state.schema;let tr=view.state.tr,pos=tr.doc.content.size;for(const raw of String(source).replace(/\r/g,'').split('\n')){const t=raw.trim();if(!t)continue;const node=nodeForLine(schema,t);tr=tr.insert(pos,node);pos+=node.nodeSize}view.dispatch(tr);return view}
function linkifyLabels(view){const type=view.state.schema.marks?.link;if(!type)return 0;const re=/^(マガジンURL|固定記事URL)：(https:\/\/note\.com\/\S+)$/;const rows=[];view.state.doc.descendants((node,pos)=>{if(!node.isTextblock)return true;const raw=node.textContent||'',m=raw.trim().match(re);if(m){const at=raw.indexOf(m[2]);if(at>=0)rows.push({start:pos+1+at,end:pos+1+at+m[2].length,url:m[2]})}return true});let tr=view.state.tr,n=0;for(const r of rows){try{tr=tr.addMark(r.start,r.end,type.create({href:r.url,target:'_blank',rel:'noopener noreferrer'}));n++}catch{try{tr=tr.addMark(r.start,r.end,type.create({href:r.url}));n++}catch{}}}if(n)view.dispatch(tr);return n}
function topRows(view){const a=[];view.state.doc.forEach((node,pos)=>a.push({node,pos,text:(node.textContent||'').trim()}));return a}
const isGroup=t=>/^(?:♾️ 無制限・制限なし|❓ 制限数の表記を確認できないマガジン|(?:🔟|[1-9]️⃣) 1日\d+記事まで)$/.test(t);
function clearAll(src,show){const view=findView();if(!view)throw new Error('EditorViewなし');const a=topRows(view);let start=-1,end=-1;for(const x of a){if(isGroup(x.text)&&start<0)start=x.pos;if(start>=0&&/horizontal/i.test(x.node.type?.name||''))end=x.pos+x.node.nodeSize}if(start<0){src.value='';show('✅ 貼付一覧なし');return}if(end<start)end=view.state.doc.content.size;view.dispatch(view.state.tr.delete(start,end));src.value='';show('🧹 ポン出し貼付分を全部削除 ✅')}
function install(){const root=document.getElementById('__mumei_pon_v14_root__');if(!root)return setTimeout(install,200);const panel=root.querySelector('#ponPanel14'),src=root.querySelector('#ponSrc14'),st=root.querySelector('#ponStatus14'),head=root.querySelector('#ponDrag14 b');if(!panel||!src||!st)return setTimeout(install,200);if(head)head.textContent='↔️ ポン出し v27';['ponMags19','ponClearList21','ponMake21','ponCards21','ponClearList22','ponMake22','ponCards22','ponClear23','ponMake23','ponCards23','ponClear24','ponMake24','ponCards24','ponClear25','ponMake25','ponCards25','ponClear26','ponMake26','ponCards26','ponClear27','ponMake27','ponCards27'].forEach(id=>document.getElementById(id)?.remove());const say=t=>st.textContent=t;
const del=document.createElement('button');del.id='ponClear27';del.textContent='🧹 ポン出し貼付分を全部消す';del.style.cssText='display:block;width:100%;border:1px solid #ff8d8d;border-radius:8px;padding:9px 5px;background:#5b1f29;color:#fff;font-weight:900;font-size:11px;margin-bottom:5px';panel.insertBefore(del,src);del.onclick=()=>{try{clearAll(src,say)}catch(e){say('❌ '+(e?.message||e))}};
const make=document.createElement('button');make.id='ponMake27';make.textContent='📚 回数整理→その場カード生成';make.style.cssText='display:block;width:100%;border:0;border-radius:8px;padding:9px 5px;background:#ffd54a;color:#261f00;font-weight:900;font-size:11px;margin-bottom:5px';panel.insertBefore(make,src);make.onclick=async()=>{if(busy)return;busy=true;make.disabled=true;try{say('参加中保存一覧を取得…');const list=await rows();if(!list.length)throw new Error('参加中一覧を取得できません');const built=build(list);src.value=built.source;const view=insertSource(src.value);linkifyLabels(view);const left=await cardifyAll(say);say(left?`❌ 生URL残り ${left}件｜停止`:'✅ マガジン＋固定記事 その場カード完成')}catch(e){say('❌ '+(e?.message||e))}finally{busy=false;make.disabled=false}};
const card=document.createElement('button');card.id='ponCards27';card.textContent='🃏 今あるURL→その場カード';card.style.cssText='display:block;width:100%;border:0;border-radius:8px;padding:9px 5px;background:#72f1c9;color:#032b25;font-weight:900;font-size:11px;margin-bottom:5px';panel.insertBefore(card,src);card.onclick=async()=>{if(busy)return;busy=true;card.disabled=true;try{const view=findView();if(view)linkifyLabels(view);const left=await cardifyAll(say);say(left?`❌ 生URL残り ${left}件｜停止`:'✅ マガジン＋固定記事 その場カード完成')}catch(e){say('❌ '+(e?.message||e))}finally{busy=false;card.disabled=false;make.disabled=false}};
}
install();
})();