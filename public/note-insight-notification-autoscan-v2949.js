(function(){
'use strict';
if(location.hostname!=='note.com')return;
const VERSION='2.9.49',INGEST='https://xxhaerjvrgmnadxjqetz.supabase.co/functions/v1/insight-notification-ingest-v2';
const TOKEN='mumei_insight_notification_sync_token_v2:',SAVED='mumei_insight_notification_saved_v2919:',CHECK='mumei_insight_notification_checkpoint_v2922:';
const FRAME='mumei-v2948-frame',ITEM='.m-navbarNoticeItem,[class*="navbarNoticeItem"],[class*="notificationItem" i],[class*="noticeItem" i],[data-testid*="notification-item" i],[data-testid*="notice-item" i]',SRC='note-notification-manual-sync-v2949';
const MAX_NEW=120,BATCH=25,STEP_WAIT=360,MAX_STEPS=90,STALL_LIMIT=5;
const clean=v=>String(v||'').replace(/<[^>]*>/g,' ').replace(/\s+/g,' ').trim(),stripTime=v=>clean(v).replace(/\s(?:たった今|昨日|\d+\s*(?:秒|分|時間|日|週|か月|ヶ月|月|年)前)$/u,'').trim(),modern=()=>Boolean(globalThis.GM),key=(p,id)=>p+String(id||'').toLowerCase();
const sleep=ms=>new Promise(r=>setTimeout(r,ms));
async function get(k,d){if(modern()&&typeof GM.getValue==='function')return GM.getValue(k,d);if(typeof GM_getValue==='function')return GM_getValue(k,d);return d}
async function set(k,v){if(modern()&&typeof GM.setValue==='function')return GM.setValue(k,v);if(typeof GM_setValue==='function')return GM_setValue(k,v)}
function request(url,body,headers={}){return new Promise((resolve,reject)=>{const fn=modern()&&typeof GM.xmlHttpRequest==='function'?GM.xmlHttpRequest:typeof GM_xmlhttpRequest==='function'?GM_xmlhttpRequest:null;if(!fn)return reject(new Error('USERSCRIPT_REQUEST_UNAVAILABLE'));fn({method:'POST',url,headers:{'Content-Type':'application/json',...headers},data:JSON.stringify(body),timeout:45000,onload:r=>{let p={};try{p=JSON.parse(r.responseText||'{}')}catch{};r.status>=200&&r.status<300?resolve(p):reject(new Error(p.error||`HTTP_${r.status}`))},onerror:()=>reject(new Error('NETWORK_ERROR')),ontimeout:()=>reject(new Error('TIMEOUT'))})})}
async function account(){try{const r=await fetch('/api/v2/current_user',{credentials:'include',cache:'no-store'});if(!r.ok)return null;const j=await r.json(),u=(j.data??j).user||(j.data??j),id=String(u.urlname||u.url_name||u.username||'').toLowerCase();return/^[a-z0-9_-]+$/.test(id)?{id}:null}catch{return null}}
function shown(el){if(!el?.getBoundingClientRect)return false;const r=el.getBoundingClientRect(),s=getComputedStyle(el);return r.width>0&&r.height>0&&s.display!=='none'&&s.visibility!=='hidden'}
function rows(root){if(!root)return[];let xs=[...root.querySelectorAll(ITEM)];if(!xs.length)xs=[...root.querySelectorAll('li,[role="listitem"],article')].filter(el=>/(?:さん|新しい記事|マガジン|フォロー|コメント|スキ|購入|メンバーシップ|掲示板)/u.test(clean(el.textContent)));return[...new Set(xs)]}
function findPanel(){const candidates=[...document.querySelectorAll('[class*="navbarNotice"],[data-testid*="notification" i],[data-testid*="notice" i],[role="dialog"],[role="menu"],[popover]')].filter(shown);return candidates.sort((a,b)=>rows(b).length-rows(a).length)[0]||null}
function noteLinks(el){return[...el.querySelectorAll('a[href]')].map(a=>{try{const u=new URL(a.getAttribute('href'),location.href);return u.hostname.endsWith('note.com')?{a,u:u.href,t:clean(a.textContent)}:null}catch{return null}}).filter(Boolean)}
function creatorIdFromUrl(v){try{const u=new URL(String(v||''),location.href),p=u.pathname.split('/').filter(Boolean);if(!u.hostname.endsWith('note.com')||p.length!==1)return'';const id=(p[0]||'').toLowerCase();return/^[a-z0-9_-]+$/.test(id)&&!['settings','sitesettings','membership','notifications'].includes(id)?id:''}catch{return''}}
function actorLink(ls,raw){return ls.find(x=>creatorIdFromUrl(x.u))||null}
function targetLink(ls){return ls.find(x=>/\/membership(?:[/?#]|$)|\/memberships?\/|\/m\/|\/n\/|kind=board_reply_comment|scrollpos=comment/i.test(x.u))||null}
function actorImage(el,actor){const direct=actor?.a?.querySelector?.('img[src]');if(direct&&direct.currentSrc&&!/magazine_cover/i.test(direct.currentSrc))return direct.currentSrc;for(const img of el.querySelectorAll('img[src]')){const src=String(img.currentSrc||img.src||'');if(/\/profile_/i.test(src))return src}for(const img of el.querySelectorAll('img[src]')){const src=String(img.currentSrc||img.src||'');if(src&&!/magazine_cover|ogp|cover/i.test(src))return src}return null}
function rowData(el){const raw=clean(el.innerText||el.textContent);if(!raw||raw.length>900)return null;const ls=noteLinks(el),target=targetLink(ls),actor=actorLink(ls,raw),tm=el.querySelector('time[datetime]')?.getAttribute('datetime')||el.querySelector('[datetime]')?.getAttribute('datetime')||null,m=stripTime(raw).match(/^(.{1,180}?)\s*さん(?:他\d+名)?(?:が|の|から|より|に)/u);return{raw_text:raw,actor_name:actor?.t||m?.[1]||null,actor_url:actor?.u||null,actor_image_url:actorImage(el,actor),target_title:target?.t||null,target_url:target?.u||null,source_url:target?.u||actor?.u||'https://note.com/',occurred_at:tm,meta:{source:SRC,userscript:VERSION}}}
const signature=r=>[stripTime(r.raw_text),String(r.target_url||'').split('#')[0],String(r.actor_url||'').split('?')[0]].join('|');
function scrollHost(panel){const list=[panel,...panel.querySelectorAll('div,ul,section')].filter(el=>{const s=getComputedStyle(el);return/(auto|scroll)/.test(s.overflowY)&&el.scrollHeight>el.clientHeight+30});return list.sort((a,b)=>(b.scrollHeight-b.clientHeight)-(a.scrollHeight-a.clientHeight))[0]||panel}
function health(text,cls=''){try{const f=document.getElementById(FRAME),h=f?.contentDocument?.getElementById('health');if(h){h.textContent=text;h.className='health '+cls}const b=f?.contentDocument?.getElementById('read');if(b)b.textContent=cls==='saving'?'自動読込中…':'自動保存（前回まで）'}catch{}}
async function sendBatch(rows,a){if(!rows.length)return[];const token=String(await get(key(TOKEN,a.id),'')||'');if(!token)throw new Error('本人連携の更新が必要です');const confirmed=[];for(let i=0;i<rows.length;i+=BATCH){const part=rows.slice(i,i+BATCH),payload=part.map(r=>({...r,meta:{...(r.meta||{}),client_signature:signature(r)}})),p=await request(INGEST,{noteId:a.id,notifications:payload},{'X-Ingest-Token':token});confirmed.push(...(Array.isArray(p.confirmedClientSignatures)?p.confirmedClientSignatures:[]).map(String))}return[...new Set(confirmed)]}
async function autoScan(){const panel=findPanel();if(!panel){health('🔔の通知一覧を開いてください','error');return}const a=await account();if(!a){health('noteのログイン状態を確認してください','error');return}const cp=await get(key(CHECK,a.id),{}),boundary=String(cp?.boundarySignature||''),savedRaw=await get(key(SAVED,a.id),[]),saved=new Set((Array.isArray(savedRaw)?savedRaw:[]).map(String)),host=scrollHost(panel),original=host.scrollTop,ordered=[],seen=new Set();let reached=false,recovered=false,boundarySig=boundary,stall=0,lastExtent=-1;host.scrollTop=0;await sleep(250);health('前回保存位置まで自動読込中…','saving');try{for(let step=0;step<MAX_STEPS;step++){const current=rows(panel).map(rowData).filter(Boolean);for(const r of current){const s=signature(r);if(!seen.has(s)){seen.add(s);ordered.push(r)}if(boundary&&s===boundary){reached=true;boundarySig=s;break}if(boundary&&!reached&&saved.has(s)){reached=true;recovered=true;boundarySig=s;break}}health(`自動読込中…確認 ${ordered.length}件${reached?'・前回位置到達':''}`,'saving');if(reached||(!boundary&&ordered.length>=MAX_NEW))break;const extent=host.scrollHeight-host.clientHeight;if(extent<=0)break;const before=host.scrollTop;host.scrollTop=Math.min(extent,before+Math.max(220,host.clientHeight*.82));await sleep(STEP_WAIT);const nowExtent=host.scrollHeight-host.clientHeight;if(Math.abs(host.scrollTop-before)<2&&nowExtent===lastExtent)stall++;else stall=0;lastExtent=nowExtent;if(stall>=STALL_LIMIT)break}const stopIndex=boundarySig?ordered.findIndex(r=>signature(r)===boundarySig):-1,scope=stopIndex>=0?ordered.slice(0,stopIndex):ordered,unique=[];for(const r of scope){const s=signature(r);if(saved.has(s))continue;unique.push(r);if(unique.length>=MAX_NEW)break}const confirmed=await sendBatch(unique,a);for(const s of confirmed)saved.add(s);await set(key(SAVED,a.id),[...saved].slice(-10000));const first=ordered[0],scopeComplete=unique.length<MAX_NEW,newBoundary=(reached||!boundary)&&scopeComplete&&first?signature(first):boundary,next={...cp,lastSaveAt:confirmed.length?Date.now():Number(cp?.lastSaveAt||0),lastCheckAt:Date.now(),savedCount:saved.size,lastError:'',manualNewCount:confirmed.length,manualSeenCount:ordered.length,version:VERSION,boundarySignature:newBoundary,boundaryAt:newBoundary!==boundary?Date.now():Number(cp?.boundaryAt||0),boundarySource:recovered?'auto-saved-overlap-v2949':'auto-checkpoint-v2949'};await set(key(CHECK,a.id),next);const tail=reached?'・前回位置まで自動確認':boundary?'・前回位置未到達':'・初回範囲';health(`保存完了 ✓ INSIGHT反映${confirmed.length}件・確認${ordered.length}件${tail}`,'done');setTimeout(()=>health(`最終保存 ${new Intl.DateTimeFormat('ja-JP',{timeZone:'Asia/Tokyo',month:'2-digit',day:'2-digit',hour:'2-digit',minute:'2-digit'}).format(new Date(next.lastSaveAt||Date.now()))}・保存済${saved.size}件`),2600)}catch(e){await set(key(CHECK,a.id),{...cp,lastError:String(e?.message||e),lastCheckAt:Date.now(),version:VERSION});health(`⚠ ${e?.message||e}`,'error')}finally{host.scrollTop=original}}
function bind(){try{const f=document.getElementById(FRAME),d=f?.contentDocument,b=d?.getElementById('read');if(!d||!b)return false;if(b.dataset.mumeiAutoScan==='1')return true;b.dataset.mumeiAutoScan='1';b.textContent='自動保存（前回まで）';d.addEventListener('click',e=>{const t=e.target;if(!(t instanceof d.defaultView.HTMLElement)||t.id!=='read')return;e.preventDefault();e.stopPropagation();e.stopImmediatePropagation();void autoScan()},true);return true}catch{return false}}
let tries=0;const timer=setInterval(()=>{if(bind()||tries++>120)clearInterval(timer)},250);window.addEventListener('pageshow',()=>{tries=0;setTimeout(bind,300)});
})();
