// ==UserScript==
// @name         無名S note INSIGHT 本人通知・統計連携
// @namespace    https://github.com/mumei-s/note-insight/notification-sync
// @version      2.9.2
// @description  note閲覧を邪魔しない手動連携。🔔通知と公式Dashboardは専用ボタンを押した時だけINSIGHTへ保存します。
// @match        https://note.com/*
// @run-at       document-idle
// @grant        GM.xmlHttpRequest
// @grant        GM.getValue
// @grant        GM.setValue
// @connect      xxhaerjvrgmnadxjqetz.supabase.co
// @updateURL    https://raw.githubusercontent.com/mumei-s/note-insight/main/public/note-insight-notification-sync.user.js
// @downloadURL  https://raw.githubusercontent.com/mumei-s/note-insight/main/public/note-insight-notification-sync.user.js
// ==/UserScript==
(function(){
'use strict';
const VERSION='2.9.2';
const PAIR='https://xxhaerjvrgmnadxjqetz.supabase.co/functions/v1/insight-notification-import-token';
const INGEST='https://xxhaerjvrgmnadxjqetz.supabase.co/functions/v1/insight-notification-ingest-v2';
const DASH='https://xxhaerjvrgmnadxjqetz.supabase.co/functions/v1/insight-dashboard-data';
const TOKEN='mumei_insight_notification_sync_token_v2:';
const MUTES='mumei_insight_magazine_mute_ids_v5:';
const FILTER='mumei_insight_magazine_filter_enabled_v3:';
const PROFILE='mumei_insight_magazine_mute_profiles_v5:';
const GROUPS='mumei_insight_notification_groups_v1:';
const clean=v=>String(v||'').replace(/\s+/g,' ').trim();
const sleep=ms=>new Promise(r=>setTimeout(r,ms));
const key=(p,id)=>p+String(id||'').toLowerCase();
const ACTION=/(?:さん(?:他\d+名)?が|あなた(?:の|を|に)|新しい(?:スキ|コメント|フォロワー)).{0,260}(?:スキしました|コメントしました|返信しました|フォローしました|フォローされました|追加しました|仲間入りしました|参加しました|購入されました|購入しました|話題|高評価|ポイント)|(?:メンバーシップ|掲示板).{0,160}(?:投稿しました|開始しました|始めました|はじめました|追加しました|公開しました|参加しました|メンバーになりました)|(?:購入がありました|返信がありました|コメントがありました)/u;
let accountId='',token='',panelObserver=null,routeTimer=0,panelTimer=0;
function gm(){if(!globalThis.GM)throw new Error('USERSCRIPT_API_UNAVAILABLE');return globalThis.GM}
async function get(k,d){return await gm().getValue(k,d)}
async function set(k,v){return await gm().setValue(k,v)}
function request(url,body,headers={}){return new Promise((resolve,reject)=>gm().xmlHttpRequest({method:'POST',url,headers:{'Content-Type':'application/json',...headers},data:JSON.stringify(body),timeout:45000,onload:r=>{let p={};try{p=JSON.parse(r.responseText||'{}')}catch{};r.status>=200&&r.status<300?resolve(p):reject(new Error(p.error||`HTTP_${r.status}`))},onerror:()=>reject(new Error('NETWORK_ERROR')),ontimeout:()=>reject(new Error('TIMEOUT'))}))}
function noteId(v){try{const u=new URL(String(v||''),location.origin),id=u.pathname.split('/').filter(Boolean)[0]||'';return/(^|\.)note\.com$/.test(u.hostname)&&/^[A-Za-z0-9_-]+$/.test(id)?id.toLowerCase():''}catch{return''}}
async function currentAccount(){try{const r=await fetch('/api/v2/current_user',{credentials:'include',headers:{accept:'application/json'},cache:'no-store'});if(!r.ok)return null;const j=await r.json(),d=j?.data??j??{},u=d.user||d,id=String(u.urlname||u.url_name||u.username||'').toLowerCase();return/^[a-z0-9_-]+$/.test(id)?{id,name:String(u.nickname||u.name||id)}:null}catch{return null}}
async function ready(){for(let i=0;i<20;i++){const a=await currentAccount();if(a)return a;await sleep(500)}return null}
async function pair(a){const u=new URL(location.href),code=(u.searchParams.get('mumei_pair')||'').replace(/\D/g,'').slice(0,8),expected=String(u.searchParams.get('mumei_account')||'').toLowerCase();let t=String(await get(key(TOKEN,a.id),'')||'');if(code){if(expected&&expected!==a.id)throw new Error(`連携先 @${expected} とログイン中 @${a.id} が違います`);const p=await request(PAIR,{action:'pair-exchange',code});t=String(p.ingestToken||'');if(!t)throw new Error('PAIR_TOKEN_NOT_RETURNED');await set(key(TOKEN,a.id),t);u.searchParams.delete('mumei_pair');u.searchParams.delete('mumei_account');history.replaceState(null,'',u.pathname+(u.search?'?'+u.searchParams.toString():'')+u.hash)}return t}
function injectStyle(){if(document.getElementById('mumei-v292-style'))return;const s=document.createElement('style');s.id='mumei-v292-style';s.textContent=`
.mumei-muted-v292{display:none!important}
#mumei-insight-panel-action,#mumei-insight-dashboard-action,#mumei-insight-dashboard-dock,#mumei-insight-status-dock,#mumei-insight-flash{display:none!important}
#mumei-v292-actions{display:flex;align-items:center;gap:5px;padding:6px 7px;margin:0;position:sticky;top:0;z-index:80;background:rgba(17,28,40,.98);border-bottom:1px solid #30465b}
#mumei-v292-actions .state{margin-right:auto;color:#91a5b8;font:800 9px/1.35 system-ui;white-space:nowrap}
#mumei-v292-actions button{border:1px solid #4d6d86;border-radius:999px;background:#142638;color:#a9edff;padding:6px 9px;font:850 9px/1.2 system-ui;white-space:nowrap}
#mumei-v292-dashboard{display:block;position:sticky;top:8px;z-index:120;border:1px solid #5a8aa7;border-radius:12px;background:#10283a;color:#b8f2ff;margin:10px auto;max-width:680px;width:calc(100% - 24px);padding:11px 12px;text-align:center;font:900 12px/1.45 system-ui;box-shadow:0 6px 24px rgba(0,0,0,.24)}
@media(max-width:520px){#mumei-v292-actions{gap:4px;padding:5px}#mumei-v292-actions .state{font-size:8px}#mumei-v292-actions button{font-size:8px;padding:5px 6px}}
`;document.documentElement.appendChild(s)}
function removeLegacyUi(){for(const id of ['mumei-insight-panel-action','mumei-insight-dashboard-action','mumei-insight-dashboard-dock','mumei-insight-status-dock','mumei-insight-flash','mumei-v291-actions','mumei-v291-dashboard'])document.getElementById(id)?.remove();for(const el of [...document.querySelectorAll('button,[id*="mumei-insight"],[class*="mumei-insight"]')]){const t=clean(el.textContent);if(t==='🔔同期'||t==='一時表示'||t.includes('INSIGHT整理済'))el.remove()}}
function visible(el){if(!el?.getBoundingClientRect)return false;const r=el.getBoundingClientRect(),s=getComputedStyle(el);return r.width>0&&r.height>0&&s.display!=='none'&&s.visibility!=='hidden'}
function links(el){return[...(el?.matches?.('a[href]')?[el]:[]),...(el?.querySelectorAll?.('a[href]')||[])]}
function text(el){return clean(el?.innerText||el?.textContent)}
function strictItem(el){const t=text(el);if(t.length<5||t.length>420||!ACTION.test(t))return false;const hasTime=Boolean(el.querySelector('time,[datetime]'))||/(?:たった今|昨日|\d+\s*(?:秒|分|時間|日|週|か月|ヶ月|月|年)前)$/u.test(t);const hasNote=links(el).some(a=>{try{return new URL(a.getAttribute('href'),location.href).hostname.endsWith('note.com')}catch{return false}});return hasTime&&hasNote}
function items(root){if(!root)return[];let xs=[...root.querySelectorAll('.m-navbarNoticeItem,[class*="navbarNoticeItem"],[data-testid*="notification-item" i],[data-testid*="notice-item" i]')];if(!xs.length)xs=[...root.querySelectorAll('li,[role="listitem"]')].filter(strictItem);return[...new Set(xs)].filter(strictItem)}
function notificationRoot(){const strong=[...document.querySelectorAll('[class*="navbarNotice"],[data-testid*="notification" i],[data-testid*="notice" i]')].filter(x=>visible(x)&&x!==document.body&&x!==document.documentElement),scored=strong.map(el=>({el,n:items(el).length})).filter(x=>x.n>0).sort((a,b)=>b.n-a.n);if(scored[0])return scored[0].el;const dialogs=[...document.querySelectorAll('[role="dialog"]')].filter(x=>visible(x)&&/(通知|お知らせ)/.test(text(x).slice(0,100))),fallback=dialogs.map(el=>({el,n:items(el).length})).filter(x=>x.n>0).sort((a,b)=>b.n-a.n);return fallback[0]?.el||null}
function isMag(t){return(/(共同)?マガジン/.test(t)&&/(追加しました|投稿しました|仲間入りしました|新しい記事)/.test(t))||/新しい記事を\s*\d*\s*本?追加しました/.test(t)}
async function applyFilter(root){const enabled=Boolean(await get(key(FILTER,accountId),false)),rawIds=await get(key(MUTES,accountId),[]),rawProfiles=await get(key(PROFILE,accountId),[]),ids=new Set((Array.isArray(rawIds)?rawIds:[]).map(noteId).filter(Boolean)),ps=Array.isArray(rawProfiles)?rawProfiles:[];for(const e of items(root)){e.classList.remove('mumei-insight-magazine-muted','mumei-muted-v290','mumei-muted-v291','mumei-muted-v292');if(!enabled||!ids.size)continue;const all=text(e)+' '+links(e).map(a=>clean(a.textContent)).join(' '),actors=links(e).map(a=>noteId(a.getAttribute('href'))).filter(Boolean);if(isMag(text(e))&&(actors.some(x=>ids.has(x))||ps.some(p=>p?.name&&all.includes(p.name))))e.classList.add('mumei-muted-v292')}}
function extract(root){const out=new Map();for(const e of items(root)){const raw=text(e),ls=links(e).map(a=>{try{return{u:new URL(a.getAttribute('href'),location.href).href,t:clean(a.innerText),el:a}}catch{return null}}).filter(Boolean),actor=ls.find(x=>/^https:\/\/note\.com\/[A-Za-z0-9_-]+\/?(?:[?#].*)?$/.test(x.u)),target=ls.find(x=>x.u.startsWith('https://note.com/')&&x.u!==actor?.u),tm=e.querySelector('time[datetime]')?.getAttribute('datetime')||e.querySelector('[datetime]')?.getAttribute('datetime')||null,img=actor?.el?.querySelector?.('img')||e.querySelector('img'),sig=[raw,actor?.u||'',target?.u||'',tm||''].join('|');if(!out.has(sig))out.set(sig,{raw_text:raw,actor_name:actor?.t||clean(img?.alt)||null,actor_url:actor?.u||null,actor_image_url:img?.currentSrc||img?.src||null,target_title:target?.t||null,target_url:target?.u||null,source_url:target?.u||'https://note.com/',occurred_at:tm,meta:{source:'note-notification-explicit-sync-v292',via:'notification-panel-manual',userscript:VERSION}})}return[...out.values()]}
async function saveRows(rows,state){if(!token)throw new Error('INSIGHT本人通知が未連携です');if(!rows.length){state.textContent='通知0件｜保存なし';return}let inserted=0,updated=0,skipped=0;for(let i=0;i<rows.length;i+=100){const p=await request(INGEST,{noteId:accountId,notifications:rows.slice(i,i+100)},{'X-Ingest-Token':token});inserted+=Number(p.inserted||0);updated+=Number(p.updated||0);skipped+=Number(p.skipped||0)+Number(p.blocked||0)}state.textContent=`保存済｜確認${rows.length}・新規${inserted}${updated?`・更新${updated}`:''}${skipped?`・除外${skipped}`:''}`}
async function saveVisible(root,state){await saveRows(extract(root),state)}
function scrollBox(root){const xs=[root,...root.querySelectorAll('*')];return xs.find(x=>x!==document.body&&x!==document.documentElement&&x.scrollHeight>x.clientHeight+80&&/(auto|scroll)/.test(getComputedStyle(x).overflowY))||null}
async function savePast(root,state){const box=scrollBox(root);if(!box){await saveVisible(root,state);return}const original=box.scrollTop,all=new Map();let stable=0,last=-1;try{for(let i=0;i<50;i++){for(const r of extract(root))all.set([r.raw_text,r.actor_url,r.target_url,r.occurred_at].join('|'),r);stable=all.size===last?stable+1:0;if(stable>=3)break;last=all.size;box.scrollTop=box.scrollHeight;await sleep(320)}for(const r of extract(root))all.set([r.raw_text,r.actor_url,r.target_url,r.occurred_at].join('|'),r)}finally{box.scrollTop=original}await saveRows([...all.values()],state)}
async function setupPanel(){const root=notificationRoot();if(!root)return;injectStyle();removeLegacyUi();await applyFilter(root);if(root.querySelector('#mumei-v292-actions'))return;const bar=document.createElement('div');bar.id='mumei-v292-actions';const st=document.createElement('span');st.className='state';const enabled=Boolean(await get(key(FILTER,accountId),false)),rawIds=await get(key(MUTES,accountId),[]),groups=await get(key(GROUPS,accountId),[]),ids=Array.isArray(rawIds)?rawIds:[],gc=Array.isArray(groups)?groups.filter(g=>g&&g.enabled!==false&&Array.isArray(g.ids)&&g.ids.length).length:0;st.textContent=enabled&&ids.length?`閲覧のみ｜整理ON ${ids.length}人${gc?` / ${gc}グループ`:''}`:'閲覧のみ｜整理OFF';const one=document.createElement('button');one.type='button';one.textContent='🔔 表示通知を読み込む';const past=document.createElement('button');past.type='button';past.textContent='↧ 過去通知を読み込む';one.onclick=async ev=>{ev.stopPropagation();one.disabled=true;try{st.textContent='保存中…';await saveVisible(root,st)}catch(e){st.textContent=`保存失敗｜${e.message||e}`}finally{one.disabled=false}};past.onclick=async ev=>{ev.stopPropagation();past.disabled=true;try{st.textContent='過去保存中…';await savePast(root,st)}catch(e){st.textContent=`保存失敗｜${e.message||e}`}finally{past.disabled=false}};bar.append(st,one,past);root.prepend(bar);if(panelObserver)panelObserver.disconnect();panelObserver=new MutationObserver(()=>{clearTimeout(panelTimer);panelTimer=setTimeout(()=>void applyFilter(root),160)});panelObserver.observe(root,{childList:true,subtree:true})}
function pickRows(j){const d=j?.data??j??{};for(const k of ['note_stats','noteStats','notes','articles','items','contents','stats'])if(Array.isArray(d?.[k]))return d[k];if(Array.isArray(j))return j;return[]}
function statRow(x){const r=x?.note||x?.article||x||{},k=String(r.key||r.note_key||r.noteKey||r.id||''),title=clean(r.name||r.title||r.note_name||'無題の記事'),views=Number(r.pv??r.view_count??r.viewCount??r.read_count??r.readCount??r.views??0),comments=Number(r.comment_count??r.commentCount??r.comments??0),likes=Number(r.like_count??r.likeCount??r.likes??0);return k?{key:k,title,url:r.url||r.note_url||`https://note.com/${accountId}/n/${k}`,views:Number.isFinite(views)?views:0,comments:Number.isFinite(comments)?comments:0,likes:Number.isFinite(likes)?likes:0}:null}
async function dashboardRows(){const map=new Map();let noNew=0;for(let page=1;page<=200;page++){const before=map.size,r=await fetch(`/api/v1/stats/pv?filter=all&page=${page}&sort=pv`,{credentials:'include',headers:{accept:'application/json'},cache:'no-store'});if(!r.ok)throw new Error(`Dashboard API ${r.status}`);const j=await r.json(),raw=pickRows(j);for(const x of raw){const y=statRow(x);if(y)map.set(y.key,y)}const d=j?.data??j??{},last=d.last_page??d.is_last_page??d.isLastPage??j.last_page??j.is_last_page??j.isLastPage;noNew=map.size===before?noNew+1:0;if(!raw.length||last===true||noNew>=2)break;await sleep(35)}return[...map.values()]}
function dashboardTotals(rows){const t=clean(document.body.innerText),read=label=>{const m=t.match(new RegExp(label+'\\s*([0-9,]+)'));return m?Number(m[1].replace(/,/g,'')):null},sum=k=>rows.reduce((a,x)=>a+Number(x[k]||0),0);return{views:read('全体ビュー')??sum('views'),comments:read('コメント')??sum('comments'),likes:read('スキ')??sum('likes')}}
function period(){const t=clean(document.body.innerText),m=t.match(/(20\d{2})年(\d{1,2})月(\d{1,2})日\s*-\s*(20\d{2})年(\d{1,2})月(\d{1,2})日/),fmt=(y,mo,d)=>`${y}-${String(mo).padStart(2,'0')}-${String(d).padStart(2,'0')}`;return m?{periodType:'custom',periodStart:fmt(m[1],m[2],m[3]),periodEnd:fmt(m[4],m[5],m[6])}:{periodType:'all',periodStart:null,periodEnd:null}}
async function saveDashboard(btn){if(!token)throw new Error('INSIGHT本人通知・統計が未連携です');btn.disabled=true;btn.textContent='Dashboardを読み取り中…';try{const rows=await dashboardRows();if(!rows.length)throw new Error('Dashboardの記事表を読み取れませんでした');btn.textContent=`${rows.length}記事をINSIGHTへ反映中…`;const p=await request(DASH,{action:'ingest',noteId:accountId,articles:rows,totals:dashboardTotals(rows),...period(),pages:Math.ceil(rows.length/20),source:'note-dashboard-manual-v292'},{'X-Ingest-Token':token});btn.textContent=`✓ 反映完了｜${p.articleCount||rows.length}記事`;setTimeout(()=>{btn.disabled=false;btn.textContent='📊 Dashboardを読み取る → INSIGHT反映'},4000)}catch(e){btn.disabled=false;btn.textContent=`読取失敗｜${e.message||e}`}}
function ensureDashboard(){if(!location.pathname.toLowerCase().startsWith('/sitesettings/stats')){document.getElementById('mumei-v292-dashboard')?.remove();return}injectStyle();if(document.getElementById('mumei-v292-dashboard'))return;const host=document.querySelector('main')||document.body;if(!host)return;const b=document.createElement('button');b.id='mumei-v292-dashboard';b.type='button';b.textContent='📊 Dashboardを読み取る → INSIGHT反映';b.onclick=()=>void saveDashboard(b);host.prepend(b)}
function decode64(raw){const b=raw.replace(/-/g,'+').replace(/_/g,'/').padEnd(Math.ceil(raw.length/4)*4,'=');return JSON.parse(decodeURIComponent(escape(atob(b))))}
async function refreshProfiles(ids){const ps=[];for(const id of ids.slice(0,500)){try{const r=await fetch(`/api/v2/creators/${encodeURIComponent(id)}`,{credentials:'include',headers:{accept:'application/json'},cache:'no-store'}),j=await r.json(),d=j?.data??j??{};ps.push({id,name:clean(d.nickname||d.name||'')})}catch{}}await set(key(PROFILE,accountId),ps)}
async function importFilter(){const u=new URL(location.href);if(u.searchParams.get('mumei_filter_reset')==='1'){await set(key(MUTES,accountId),[]);await set(key(PROFILE,accountId),[]);await set(key(GROUPS,accountId),[]);await set(key(FILTER,accountId),false);u.searchParams.delete('mumei_filter_reset');history.replaceState(null,'',u.pathname+(u.search?'?'+u.searchParams.toString():'')+u.hash);return}const grouped=u.searchParams.get('mumei_groups_sync');if(grouped){try{const groups=(decode64(grouped)||[]).map(g=>({name:clean(g?.name)||'グループ',enabled:g?.enabled!==false,ids:[...new Set((Array.isArray(g?.ids)?g.ids:[]).map(noteId).filter(Boolean))]})).filter(g=>g.ids.length),ids=[...new Set(groups.filter(g=>g.enabled).flatMap(g=>g.ids))];await set(key(GROUPS,accountId),groups);await set(key(MUTES,accountId),ids);await set(key(FILTER,accountId),ids.length>0);await refreshProfiles(ids)}catch{}u.searchParams.delete('mumei_groups_sync');u.searchParams.delete('mumei_account');history.replaceState(null,'',u.pathname+(u.search?'?'+u.searchParams.toString():'')+u.hash);return}const raw=u.searchParams.get('mumei_mute_sync');if(!raw)return;try{const ids=[...new Set((decode64(raw)||[]).map(noteId).filter(Boolean))];await set(key(MUTES,accountId),ids);await set(key(FILTER,accountId),ids.length>0);await set(key(GROUPS,accountId),ids.length?[{name:'登録済み',enabled:true,ids}]:[]);await refreshProfiles(ids);u.searchParams.delete('mumei_mute_sync');u.searchParams.delete('mumei_account');history.replaceState(null,'',u.pathname+(u.search?'?'+u.searchParams.toString():'')+u.hash)}catch{}}
function clickHook(ev){const el=ev.target instanceof Element?ev.target:null;if(!el)return;const button=el.closest('button,[role="button"],[data-testid]');if(!button)return;const s=clean((button.getAttribute('aria-label')||'')+' '+(button.getAttribute('data-testid')||'')+' '+button.textContent);if(/通知|お知らせ|notification|notice/i.test(s)){setTimeout(()=>void setupPanel(),180);setTimeout(()=>void setupPanel(),650)}}
async function init(){injectStyle();removeLegacyUi();const a=await ready();if(!a)return;accountId=a.id;try{token=await pair(a)}catch{}await importFilter();ensureDashboard();void setupPanel();document.addEventListener('click',clickHook,true);routeTimer=window.setInterval(()=>{ensureDashboard();const maybe=document.querySelector('[class*="navbarNotice"],[data-testid*="notification" i],[data-testid*="notice" i]');if(maybe&&visible(maybe))void setupPanel()},1200)}
void init();
})();
