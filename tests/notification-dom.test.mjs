import test from 'node:test';
import assert from 'node:assert/strict';
import fs from 'node:fs';
import {JSDOM} from 'jsdom';
const read=p=>fs.readFileSync(new URL('../public/'+p,import.meta.url),'utf8');
const dock=read('note-insight-notification-dock-watch-v312.js');
const reader=read('note-insight-notification-reader-v322.js');
const v3=read('note-insight-notification-v3.user.js');
function env(){const dom=new JSDOM('<button id="bell" aria-label="通知">🔔</button><main><a class="background">背景の記事です</a></main><section id="popup" role="dialog" hidden><button>通知</button><button>お知らせ</button><div id="list"></div></section>',{url:'https://note.com/',runScripts:'outside-only',pretendToBeVisual:true});const w=dom.window;w.HTMLElement.prototype.getBoundingClientRect=function(){return {width:320,height:200,left:0,top:40,right:320,bottom:240}};w.fetch=async()=>({ok:true,json:async()=>({data:{urlname:'fixture'}})});const values=new Map();w.GM={getValue:async(k,d)=>values.get(k)??d,setValue:async(k,v)=>values.set(k,v)};return{dom,w,values}}
function expose(w,src,names){w.eval(src.replace(/\}\)\(\);?\s*$/,'window.testAPI={'+names.join(',')+'};})();'));return w.testAPI}
function exposeDock(w){const src=dock.replace("if(document.body)boot();else document.addEventListener('DOMContentLoaded',boot,{once:true});\n})();","window.testAPI={findShell,showDock,ensureRoot};\n})();");w.eval(src);return w.testAPI}

test('dock recognizes the opened notification shell and keeps a notifications-route fallback',()=>{const{dom,w}=env();try{w.history.replaceState(null,'','/notifications');const api=exposeDock(w),popup=w.document.getElementById('popup');popup.hidden=false;w.document.getElementById('list').innerHTML='<a class="m-navbarNoticeItem">Aさんがコメントしました3分前</a>';assert.equal(api.findShell().id,'popup');api.showDock(true);const root=w.document.querySelector('[data-mumei-notification-dock="1"]');assert.ok(root);assert.equal(root.querySelectorAll('button').length,4);assert.equal(root.querySelector('[data-act="settings"]').textContent,'設定');popup.hidden=true;assert.equal(api.findShell().tagName,'MAIN')}finally{dom.window.close()}});
test('reader can rediscover a notification panel when the dock marker is missing',()=>{const{dom,w}=env();try{w.history.replaceState(null,'','/notifications');const popup=w.document.getElementById('popup');popup.hidden=false;w.document.getElementById('list').innerHTML='<a class="m-navbarNoticeItem" href="https://note.com/a/n/n1">Aさんがコメントしました3分前</a>';const api=expose(w,reader,['panel','rediscoverPanel']);assert.equal(api.panel().id,'popup');assert.equal(popup.getAttribute('data-mumei-notice-shell-v3'),'1')}finally{dom.window.close()}});
test('direct reader extracts rows and sends oldest-to-newest without iframe forwarding',async()=>{const{dom,w,values}=env();try{const popup=w.document.getElementById('popup');popup.hidden=false;popup.setAttribute('data-mumei-notice-shell-v3','1');w.document.getElementById('list').innerHTML='<a class="m-navbarNoticeItem" data-notification-id="new" href="https://note.com/a/n/n1?c=c1">Aさんがあなたのコメントに返信しました3分前</a><a class="m-navbarNoticeItem" data-notification-id="old" href="https://note.com/a/m/m1">未知の通知3分前</a>';const api=expose(w,reader,['rows','rowData','panel','sendBatch','sig','scan']);assert.equal(api.panel().id,'popup');const rs=api.rows(popup);assert.equal(rs.length,2);assert.match(api.rowData(rs[0]).target_url,/n1\?c=c1/);values.set('mumei_insight_notification_sync_token_v2:fixture','fixture-only');const sent=[];w.GM.xmlHttpRequest=o=>{const batch=JSON.parse(o.data).notifications;sent.push(...batch.map(x=>x.meta.event_identity));o.onload({status:200,responseText:JSON.stringify({ok:true,confirmedClientSignatures:batch.map(api.sig)})})};await api.scan();assert.deepEqual(sent,['notice:old','notice:new']);assert.equal(values.get('mumei_insight_notification_saved_v2919:fixture').length,2)}finally{dom.window.close()}});
test('partial acknowledgement leaves only missing rows in outbox',async()=>{const{dom,w,values}=env();try{const api=expose(w,reader,['sendBatch','sig','readOutbox']),a={id:'fixture'},saved=new Set();values.set('mumei_insight_notification_sync_token_v2:fixture','fixture-only');const input=['old','new'].map(id=>({raw_text:id+'さんが返信しました',target_url:'https://note.com/a/n/n1',meta:{event_identity:'notice:'+id}}));w.GM.xmlHttpRequest=o=>o.onload({status:200,responseText:JSON.stringify({ok:true,confirmedClientSignatures:[api.sig(input[0])]})});await assert.rejects(api.sendBatch(input,a,saved),/未保存/);assert.equal(api.readOutbox('fixture').length,1);assert.equal(api.readOutbox('fixture')[0].meta.event_identity,'notice:new')}finally{dom.window.close()}});
test('account change blocks transmitting a previous-account outbox',async()=>{const{dom,w,values}=env();try{const api=expose(w,reader,['sendBatch','readOutbox']);values.set('mumei_insight_notification_sync_token_v2:fixture','fixture-only');w.fetch=async()=>({ok:true,json:async()=>({data:{urlname:'different'}})});let sent=false;w.GM.xmlHttpRequest=()=>{sent=true};await assert.rejects(api.sendBatch([{raw_text:'Aさんが返信しました',meta:{event_identity:'notice:1'}}],{id:'fixture'},new Set()),/NOTE_ACCOUNT_CHANGED/);assert.equal(sent,false);assert.equal(api.readOutbox('fixture').length,1)}finally{dom.window.close()}});
test('active notification scripts parse',()=>{for(const name of ['note-insight-notification-dock-watch-v312.js','note-insight-notification-reader-v322.js','note-insight-notification-loader-v318.js','note-insight-dashboard-integrated-v318.js','note-insight-notification-v3.user.js'])assert.doesNotThrow(()=>new Function(read(name)))});
test('parent-owned visible dock captures all four taps before page handlers',async()=>{
  const{w}=env();
  try{
    w.history.replaceState(null,'','/notifications');
    const source=v3
      .replace('void boot();','window.parentControls={startRescue};')
      .replace("location.href='https://mumei-s.github.io/note-insight/notification-filter.html?from=note&mumei_return='+encodeURIComponent('https://note.com/notifications')+'&v=329&ts='+Date.now();return","window.testDestination='settings';return")
      .replace('location.href=u.href}}',"window.testDestination='insight'}}");
    w.eval(source);
    let reads=0,filters=0;
    w.__mumeiV3Reader322={scan:async()=>{reads++}};
    w.__mumeiV3Dock322={toggleFilter:async()=>{filters++;return true}};
    w.parentControls.startRescue();
    const root=w.document.getElementById('mumei-v3-rescue-dock-v329');
    assert.ok(root);assert.equal(root.querySelectorAll('button').length,4);
    root.querySelector('[data-r="read"]').click();await new Promise(r=>setTimeout(r,10));assert.equal(reads,1);
    root.querySelector('[data-r="filter"]').click();await new Promise(r=>setTimeout(r,20));assert.equal(filters,1);assert.match(root.querySelector('[data-r="filter"]').textContent,/ON/);
    root.querySelector('[data-r="settings"]').click();assert.equal(w.testDestination,'settings');assert.match(root.querySelector('[data-r="settings"]').textContent,/設定へ/);
    root.querySelector('[data-r="ins"]').click();await new Promise(r=>setTimeout(r,20));assert.equal(w.testDestination,'insight');assert.match(root.querySelector('[data-r="ins"]').textContent,/INSIGHTへ/);
  }finally{w.close()}
});
test('all four controls respond and settings opens the filter page',async()=>{const{w}=env();try{w.history.replaceState(null,'','/notifications');const source=dock.replace("if(document.body)boot();else document.addEventListener('DOMContentLoaded',boot,{once:true});",'window.controls={ensureRoot,showDock};').replaceAll('location.href=u.href','window.testDestination=u.href');w.eval(source);w.__mumeiV3Reader322Ready=true;const root=w.controls.ensureRoot();let directReads=0,fallbackReads=0;w.__mumeiV3Reader322={scan:()=>{directReads++}};w.document.addEventListener('mumei-v3-read-request',()=>fallbackReads++);root.querySelector('[data-act="read"]').click();assert.equal(directReads,1);assert.equal(fallbackReads,0);root.querySelector('[data-act="filter"]').click();await new Promise(r=>setTimeout(r,20));assert.match(root.querySelector('[data-act="filter"]').textContent,/ON/);root.querySelector('[data-act="settings"]').click();await new Promise(r=>setTimeout(r,20));assert.match(w.testDestination,/notification-filter.html/);root.querySelector('[data-act="ins"]').click();await new Promise(r=>setTimeout(r,20));assert.match(w.testDestination,/notification-entry.html/);assert.match(w.testDestination,/account=fixture/);delete w.__mumeiV3Reader322;root.querySelector('[data-act="read"]').click();assert.equal(fallbackReads,1)}finally{w.close()}});

test('installer uses raw userscript fallback and keeps setup tab open',async()=>{const html=read('tool-setup.html'),dom=new JSDOM(html,{url:'https://mumei-s.github.io/note-insight/tool-setup.html?browser=android-edge',runScripts:'outside-only',pretendToBeVisual:true}),w=dom.window;try{w.fetch=async()=>({ok:true,json:async()=>({notificationVersion:'3.2.9',dashboardVersion:'1.4.4'})});w.eval(w.document.querySelector('script').textContent);await new Promise(r=>setTimeout(r,30));const install=w.document.getElementById('install');assert.equal(install.href,'https://raw.githubusercontent.com/mumei-s/note-insight/main/public/note-insight-notification-v3.user.js');assert.equal(install.target,'_blank');assert.equal(w.location.pathname,'/note-insight/tool-setup.html');assert.match(w.document.getElementById('currentSteps').textContent,/ユーザースクリプトの更新を確認/);assert.match(w.document.body.textContent,/URLを貼り付ける操作はありません/);const verify=w.document.getElementById('verify');assert.equal(verify.target,'_blank');assert.match(verify.href,/https:\/\/note\.com\/notifications\?mumei_insight_version_check=1/);assert.ok(w.document.getElementById('verifyResult'));assert.equal(w.document.getElementById('notice'),null);assert.equal(w.document.getElementById('dash'),null);assert.doesNotMatch(w.document.body.textContent,/通知フィルター設定|初回の連携・読み込み/)}finally{w.close()}});
test('fresh V3.2.9 runtime confirmation is shown clearly as updated',async()=>{const now=Date.now(),url=`https://mumei-s.github.io/note-insight/tool-setup.html?browser=android-edge&notificationInstalled=3.2.9&notificationCheckedAt=${now}&notificationUpdateResult=runtime-checked-v329`,dom=new JSDOM(read('tool-setup.html'),{url,runScripts:'outside-only',pretendToBeVisual:true}),w=dom.window;try{w.fetch=async()=>({ok:true,json:async()=>({notificationVersion:'3.2.9',dashboardVersion:'1.4.4'})});w.eval(w.document.querySelector('script').textContent);await new Promise(r=>setTimeout(r,30));const box=w.document.getElementById('verifyResult');assert.match(box.textContent,/更新済み/);assert.match(box.textContent,/V3\.2\.9/);assert.ok(box.classList.contains('ok'));assert.match(w.document.getElementById('versionLine').textContent,/現在：V3\.2\.9.*最新：V3\.2\.9/)}finally{w.close()}});
test('version confirmation uses note notifications and returns to setup',async()=>{const dom=new JSDOM(read('tool-setup.html'),{url:'https://mumei-s.github.io/note-insight/tool-setup.html?account=fixture&browser=android-edge',runScripts:'outside-only'}),w=dom.window;try{w.fetch=async()=>({ok:true,json:async()=>({notificationVersion:'3.2.9'})});w.eval(w.document.querySelector('script').textContent);await new Promise(r=>setTimeout(r,20));const u=new URL(w.eval('versionCheckUrl()'));assert.equal(u.pathname,'/notifications');assert.equal(u.searchParams.get('mumei_insight_version_check'),'1');const ret=new URL(u.searchParams.get('mumei_return'));assert.equal(ret.pathname,'/note-insight/tool-setup.html');assert.equal(ret.searchParams.get('browser'),'android-edge')}finally{w.close()}});
test('V3.2.9 wrapper handles version return before loading remote components',()=>{const v3=read('note-insight-notification-v3.user.js');assert.match(v3,/@version\s+3\.2\.9/);assert.match(v3,/function directVersionCheck\(\)/);assert.match(v3,/runtime-checked-v329/);assert.match(v3,/location\.replace\(dest\.href\)/);assert.ok(v3.indexOf('async function boot(){if(directVersionCheck())return')<v3.indexOf("await loadPart('note-insight-notification-dock-watch-v312.js')"));for(const key of ['@updateURL','@downloadURL'])assert.match(v3,new RegExp(key+'\\s+https://raw\\.githubusercontent\\.com/mumei-s/note-insight/main/public/note-insight-notification-v3\\.user\\.js'))});
test('browser-specific setup keeps slim route controls and selected browser',async()=>{const html=read('tool-setup.html');assert.match(html,/\.browser-grid\{display:grid;grid-template-columns:1fr;gap:3px\}/);assert.doesNotMatch(html,/grid-template-columns:repeat\(2/);const dom=new JSDOM(html,{url:'https://mumei-s.github.io/note-insight/tool-setup.html?account=fixture&browser=android-edge',runScripts:'outside-only'}),w=dom.window;try{w.fetch=async()=>({ok:true,json:async()=>({notificationVersion:'3.2.9'})});w.eval(w.document.querySelector('script').textContent);await new Promise(r=>setTimeout(r,30));const d=w.document;assert.equal(d.querySelectorAll('.browser-choice').length,9);for(const key of ['android-other','android-firefox','ios-other','ios-safari','pc-edge','pc-chrome','pc-firefox','mac-safari','android-edge']){d.querySelector(`[data-browser="${key}"]`).click();const blocked=key.endsWith('other');assert.equal(d.getElementById('updateFlow').hidden,blocked);assert.equal(d.getElementById('confirmFlow').hidden,blocked);assert.equal(d.getElementById('verify').hasAttribute('aria-disabled'),blocked);assert.equal(d.querySelectorAll('[aria-pressed="true"]').length,1);assert.equal(new URL(w.location.href).searchParams.get('browser'),key);assert.equal(new URL(w.eval('versionCheckUrl()')).searchParams.get('mumei_return').includes('browser='+key),true);assert.equal(d.getElementById('install').hasAttribute('download'),false);if(key==='ios-safari')assert.match(d.getElementById('recoverySteps').textContent,/Userscripts/)}assert.match(d.body.textContent,/URLを貼り付ける操作はありません/);assert.equal(new URL(d.getElementById('pageUrl').value).searchParams.get('account'),'fixture');assert.equal(new URL(d.getElementById('pageUrl').value).searchParams.has('browser'),false)}finally{w.close()}});
