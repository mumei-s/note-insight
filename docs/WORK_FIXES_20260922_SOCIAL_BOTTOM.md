# 相互照合を一覧の下から行う修正（2026-09-22）

ユーザーの最新指示は「フォロー・フォロワーを互いに照らし合わせて増減、特に減を知りたい」「今実装しているのを下から」。相互照合の共有実装を採用し、別の照合実装は公開しない。

- フォロー中の一覧を下のページから読み、各ページ内も下の人物から保存する。
- 画面の初期表示は「相互の照合・減を確認」「下から1,000人」。最新1,000人への切り替えを維持。
- 「相手からフォローなし」と、前回の相互フォローから「相互から減」を区別。初回から非相互だった人を過去の減少と断定しない。
- 本人のcurrent_userとプロフィールisMyselfを確認し、本人に対する実関係フラグを読む。公開応答のfalse・1,000人枠外を解除の根拠にしない。
- ページ単位の保存確認・本人切替時停止・途中再開を維持。本人のフォロー895人は全件を下から取得可能。1,000人超の一覧は取得できた範囲を逆順に読む。
- 社会関係の旧履歴画面も残し、下側はfirst_seen_atではなくsource_rankの逆順で選択。

配信版: 本体2026.09.22.7 / 本人通知3.6.5。通知wrapperにsocial compare Readerを組み込む。DM1.4.4 / Dashboard1.4.5はこの相互照合公開では据え置き。同時進行中のDM・曜日分析・戻る操作の差分は元の共有作業領域に保持している。

バックエンド:
- social_mutual_comparison_v1適用成功。
- insight-social-compare version 1 / insight-social-events version 8を配備し、取得した配備ソースがローカルと完全一致。
- custom token認証を関数内で行い、member scopeはサーバー側で確定。テーブルはRLS有効でservice_roleのみアクセス。
- SQLトランザクションで初回非相互・相互から減・古い応答の排除・繰り返し照合の日時維持・再相互・権限を検証し、合成データをrollback。
- 不採用のsocial_reciprocal_bottomプロトタイプは実データ0件を確認して削除済み。作成と削除の移行履歴のみ保存。

検証: ビルド成功。Pagesと同じ回帰テストは74件＋118件＝192件すべて成功（相互照合14件と、下から／最新／減／個別履歴の画面切替を含む）。公開配信の最終結果は追記する。実機でのnote本人ログイン・実データ全件取得は未確認。端末内の旧userscriptがWeb配信だけで更新済みになるとは報告しない。
