# DM・曜日分析・戻る操作の残り更新（2026-09-22）

基点mainはb1274d2b4a579cc3529cf892c110d9f307ebe659。ユーザー「ほかの更新はしたの？」を受け、相互照合の先行公開だけで残っていた共有修正を統合した。

配信版: 本体2026.09.22.8 / 本人通知3.6.5 / ダッシュボード同期1.4.6 / DM同期1.4.5。

DM:
- 取得元API URLと本文の会話IDを突き合わせ、今開いている別の会話へ保存しない。サーバーでも矛盾を拒否する。
- key / messageKey / message_uuidなどの実識別子と最古の日時から過去取得カーソルを進める。識別子・保存確認がないページは飛ばさない。
- 初回は過去分の取得、完了後は新着だけ。保存件数と全件保存完了・新着待ちを同じ表示に保つ。画面を移動した後の遅い応答が表示を戻さない。
- 本文の改行を維持。同じ表示名だけで別会話を同じ人にまとめない。以前の画像などの確認済みプロフィールは欠損応答で消さない。
- DM wrapperにも相互照合Readerを組み込むため、DM単独導入者も本人確認済みの照合を使える。

曜日分析:
- 日別PVの0と未取得を区別する。未取得日は平均の分母に入れず、未取得曜日は「—」と表示する。
- 保存済みの過去の日別値を使い、最新の保存に日別値がなくても消さない。後から取得した公式訂正値を優先する。
- 日付列・グラフ系列・ページ内JSONを読む。記事単位PVや流入元内訳、月別・週別集計を日別全体PVへ混ぜない。
- 不要だった曜日平均の説明文を取り除き、日別データがない場合に同期への入口を表示する。

戻る操作:
- カテゴリ移動をhistoryへ保存し、戻る・進むの両方で元のカテゴリと表示を復元。
- 親のクリック捕捉が子のタブ選択を止める処理を撤去。フォロー画面から他項目へ移動して戻る場合を実コンポーネントで検証。

バックエンド反映:
- dm_room_source_repair_v145は既に適用済み。未適用だったdm_message_source_repair_v145を適用し、取得URLで保存先を確定できた残り4件を修復。事後の同条件の会話不一致は0件。本文内容は変更せず、修復前の会話キーをmetaに保存。
- insight-dm-ingest v4 / insight-dm-feed v4 / insight-dashboard-data v8を配備。取得した配備ソースはローカルと完全一致。

検証:
- ビルド成功。Pagesと同じ回帰テスト74件＋133件＝207件、すべて成功。
- DMの保存拒否・改行・本人スコープ・同名相手の分離、ページ継続、入力欄、日別値と曜日計算、実コンポーネントの戻る・進む・お気に入り復帰を含む。
- 本物のnoteにログインした各利用端末の全件取得やスマホのキーボードは未確認。自動テスト成功を実機成功と扱わない。
- 公開後のPages・raw GitHub配信照合結果は追記する。


公開確認: 機能main `c013151beecab0fe313605c3658bdd4c2eb0e79b`、GitHub Actions `35710900824` のbuild・207件回帰・Pages deploy成功。Pagesのmanifest、DM wrapper/network/reader、Dashboard wrapper/core、相互照合Reader、通知wrapper、Service Worker、deploy-version、index、本体JSの12ファイルがHTTP200でローカルと内容完全一致。実際の@require先（DM `?v=145`、Dashboard `?v=146`）を含むraw GitHubの5ファイルも内容一致。利用端末にはDM1.4.5／Dashboard1.4.6への更新が必要。実機の本人ログイン状態での全件取得・キーボード動作は未確認を維持。
