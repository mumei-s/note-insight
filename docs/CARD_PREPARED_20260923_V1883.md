# 極薄＋通知 18.8.3

## ユーザーの確定条件

- #イケメングランプリ宵空カップの記事を先頭にする。
- 指定2記事のスキした人と人物単位で統合し、重複者は#の記事を採用する。
- #以外は今日・昨日（日本時間）の新しい記事、なければ固定、固定もなければ最新。固定なしで最新を選んだ29人は通常群の後、実績の算数の直前にまとめる。
- 最後は `https://note.com/fuku444/n/nb4f6934381e9`（実績の算数）。
- 全件の画像下キャプションに記事投稿者の表示名＋「さん」を付ける。
- 通知用の標準カードを全件作り、公開後は今回の標準カードだけを一括削除する。

## 投稿者・画像の対応

記事APIの key と user.urlname を記事URL・対象者IDと照合する。キャプションにスキ一覧の名前を推測転用しない。name/nicknameが欠けた場合・IDが不一致の場合はエラーにし、空欄のまま完了しない。事前画像は各行のPNGのSHA-256、860×140、順番、重複、最後の実績を読込時にも検証する。

note公開エディターの2026-09-23取得版 `57-2ae1cd5058b7ac1f.js` で、imageノードの `inline*` 内容が `figcaption` に出ることを確認。実際のSchemaを実行し、文字列内容と画像リンクが同一ノードからfigure/anchor/figcaptionに出力されることも確認。本文位置が変化するため、キャプションを入れる処理は画像IDで現在位置を取り直し、後ろの画像から置換する。

## 完成データの連続投入

作成済みPNGをCacheStorageへ保存する。大きい画像データをlocalStorage・本文控えに重複保存しない。スマホ側ではサムネイルの再取得・描画をせず、40枚ずつnoteの正規画像投入処理へ渡す。各組のアップロード、URL、キャプション、下書き保存を確認してから次へ進む。

現在のnote公開エディターのドラッグ＆ドロップが呼ぶ `94928.CwN` の引数と関数本文を照合し、合う場合だけ使用する。合わない場合は既存の「＋→画像」入力経路へ戻す。画像アップロード自体はnoteの処理であり、外部画像を本文へ直接捏造しない。自動公開・自動再読込・ページ移動はしない。

## 初期化の残留

18.8.2は記録に画像IDがあると、そのIDが本文に見つからなくてもsrc＋記事リンクで探さなかった。また進捗記録を失うと同じ記事の本文控えにある画像記録を利用しなかった。

18.8.3では同じ記事の控えも参照し、IDが再生成された画像は保存済みsrc＋リンクが一意に一致するときだけ削除する。重複して特定できない場合や未記録画像、通信中は記録を消さず停止する。元の本文や画像を見た目・サイズだけで削除しない。

## 検証範囲

47件のNode回帰テストに、307枚の40枚ずつ連続投入、全員のキャプション・URL、標準カード作成・一括削除後の画像保持、ID再生成後の初期化、投稿者不一致、#先頭、古い固定・固定なしの最新へのフォールバックを含む。

これはAndroidでの実アップロード成功確認ではない。約200枚で停止した実機の根本原因は、当該端末の通信エラーの記録がないため未確定。端末側の生成と大量の同時投入は減らすが、「300枚の実機成功を再確認済み」とは報告しない。

## 完成データ

日本時間2026-09-23 23:29を基準に297件。#32件、今日／昨日176件、固定59件、固定なしの最新29件、実績の算数1件。公開記事なしによる除外0、人物・記事の重複0。

全297件について、キャッシュした記事APIの投稿者ID・キー・表示名・選定条件を独立した検証スクリプトで再照合。PNGのハッシュ・860×140・行番号・最後の実績も297件すべて確認。画像は合計8,189,381バイトに圧縮、読込用JSONは11,255,522バイト。元画像の全文／本文は配布物に含めない。

公開コミット・Actionsは公開確認後に追記する。

## スキ一覧の追加分（2026-09-23）

スクリーンショット先頭の `deeplayer_misuzu` を、`n3184e99d27bf` の公開スキ一覧で確認。取得は213アカウント、匿名スキ1件を加えた画面表示214件と一致。もう一方の `nc5786f97f59b` は135アカウントで追加漏れなし。

既存297行の相対順序・画像・記事URLは保持し、深層意識ヒーリング調律師・深鈴さんの本日記事 `nfd247a92123f` を297番に追加。実績の算数は298番で最後。全298行の投稿者名、キャプション、URL、画像ハッシュ、重複なしを再検証。追加者の名前を画面表示から推測せず、選定記事の投稿者IDと名前で照合した。

## v18.8.4：#42記事を全件掲載

ユーザーの最終指定により、#記事内は投稿者単位で間引かない。タグAPIで取得した42件の一意な記事キーを順序付きで保存し、先頭42行と全一致することを検証。同一作者の別記事9件も保持する。スキ一覧側は#の投稿者との重複と人物重複を除外する。

完成データは#42記事＋スキ一覧側265記事＋実績の算数1記事＝308行・308枚（299人）。深鈴さん307番、実績の算数308番。URL重複0。308行の名前・URL・投稿者ID・画像SHA-256を照合済み。48件の回帰検証成功。実際のAndroid画像アップロードと通知到達は未確認。

旧298件ファイルは#の全記事を含まないため、今回の投稿には使わない。v18.8.4と308件データをセットで使用する。
