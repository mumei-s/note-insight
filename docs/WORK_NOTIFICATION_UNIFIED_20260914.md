# 通知統合・実装継続 2026-09-14

開始時の最新main: b5626a1fba36bc7bc0be12df68ca083bd38d9067。
9/13診断と9/14小型可動ボタン・カテゴリ収納を維持。9/8の手動保存のみの仕様には戻さない。

## 今回の実装
- 本人通知3.1.8 / INSIGHT 2026.09.14.12。ダッシュボード読取プロトコル1.4.4は維持。
- dock-watch-v312にあった余分な閉じ括弧を修正。構文検査対象に追加。
- V3入口はloader-v318へ。3.1.7の本体未確認での即時バージョン返却を外し、必須部品のロード成功後に確認結果を返す。
- 小型可動ボタン、開くと4列（読込／フィルター／設定／INSIGHT）、長押し移動は維持。
- 20件単位でサーバーが確認した署名だけを保存。各バッチで記録線の情報を更新する。
- 送信前にアカウント別outboxへ同期的に記録。中断・通信失敗・部分確認時の未送信分を次回再試行する。保存済みと送信待ちは別扱い。
- pagehide／非表示／履歴移動で捕捉済み分を保持。表示復帰で自動読込を再試行する。
- 最初の履歴取得完了を記録した後は、保存済み通知との重なりを検出したところで古い側への走査を止める。未取得履歴がある状態では重なりだけで全完了と判定しない。
- 読込開始前に排他し、送信前にはnoteの現在アカウントを再照合する。
- 記録線は通知IDとサーバー確認済み署名に対応し、rootリンクを含む通知も対象。
- V3統合パッケージへダッシュボード同期を追加。導入センターのインストールは1本。通知とダッシュボードのアカウント連携は引き続きそれぞれ照合する。
- インストール確認後は導入センターにとどまる。本人通知の連携ボタンはこのインストールへ改めて連携コードを渡す。
- 画面表示のDashboardをダッシュボードへ。API・URL・保存キーは変更しない。

## 検証
構文チェック、既存統合回帰試験に加えてDOM試験:
- 下から上の送信順、未知通知の保持。
- 部分確認後の記録線と未送信分だけの再試行。
- pagehide中の未確認分を保存済みと表示しないこと。
- 初回完了後の差分読込で最古までスクロールしないこと。
- アカウント変更時に前アカウントの未送信分を送らないこと。
- 有効部品の構文検査。

## 実機で未確認の範囲
Android実機のブラウザ描画、実際の拡張機能インストール、本人noteの全通知との照合はこの作業環境では未確認。
DOMへ届く前の通知は保存できない。画面消灯後にOSが停止した間の通信完了は保証せず、未送信分を次回送る。
日時しかない通知の同一性はnoteの提供情報に依存する。通知IDのある行を優先する。
CI/Pages結果はこのコミットのGitHub Actionsで確認する。成功するまでは公開完了と扱わない。
