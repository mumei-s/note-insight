# 2026-09-22 上段のインストール / 更新点滅

## 実機報告

ユーザー添付1000015207.jpgと補足「インストール 更新ってのが点滅」「注意事項の上」を対象とする。注意事項直上の3カードで旧リンクが出現・消滅する。前回の単独DOM補正テスト成功を実機成功とした判断は不足だった。

## 原因と修正

- 開始時main `bec1051217b88263093e7f176303051ccc6057d2`、tree `2f908e9544d41a631dbc907a49d012c8c104d0f2` を取得し、完全一致する独立作業元を作成。別作業のカード編集18.7.0も保持。
- `insight-source-boundaries.ts` から読み込む旧 `insight-ux-v13.ts` が、`インストール / 更新` のリンクと旧ラベル・高さを繰り返し生成。現行 `insight-top-install-v16.ts` が除去・修正し、相互のMutationObserverが再実行された。
- 現行上段の存在を旧補正が認識し、旧リンク・ラベル・高さ・CSSの書換を止める。現在の設定、パネルON/OFF、通常データ・分析・通知の操作を維持。
- 更新案内パネルは内容が変わったときだけ描画し直す。同じ案内を再生成する別のループを止め、更新ボタンのフォーカス・クリックを維持。

## 検証

- 実配信と同じ7つの表示処理を同時に実行する回帰テストを追加。
- 修正前は650msの静止観測中に通常状態200回、更新あり48回のDOM変化を再現。修正後は全条件0回。
- モバイル・広い画面・読み込み順反転・更新あり・未導入・更新完了・公開データ更新中・各カード操作・ON/OFF・案内ボタンのフォーカスとクリックを確認。
- 本体 `2026.09.22.10`。通知3.6.5 / Dashboard1.4.7 / DM1.4.5を維持。本件はWeb側修正なのでツールの再インストールは不要。
- GitHub Actionsの必須回帰ゲートに追加。公開後はdeploy成功と公開JS・manifestの内容一致を確認する。ユーザー端末の点滅停止は別途実機結果を優先する。

## 次のユーザー指示

点滅を先に公開し、そのまま「通常データ下に表示される本体更新案内と↑表記の紛らわしさ」を修正し、全体の不具合確認を継続する。

追加指示：ダッシュボードが読み込まれていない実機結果を優先し、公式の折りたたみパネル内も読取対象として確認・修正する。
